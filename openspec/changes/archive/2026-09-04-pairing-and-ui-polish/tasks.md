## 1. One-way pairing handshake

- [x] 1.1 Add `ChatComposer.ComposePairingAck(string role, string code, string triggerPhrase)` building `"collarpairack {role} {code} {triggerPhrase}"`, and verify it round-trips through a matching parser - implemented as `ComposePairingAck(name, world, code, triggerPhrase)` (target is already known - the request's verified sender - so it builds the full `/tell` directly rather than a bare trigger body); parses correctly via `SplitFirstToken` the same way the existing invite parses (traced by hand)
- [x] 1.2 Add a `TryHandlePairingAckMessage` path in `ChatCommandListener.OnChatMessage` (alongside the existing `collarpair` handling) that recognizes the `collarpairack` keyword, extracts role/code/trigger phrase, and verify an unrecognized-keyword tell still falls through to alias parsing unaffected - checked *before* the invite keyword since `"collarpairack"` itself starts with `"collarpair"` (traced by hand: a genuine invite's next character after "collarpair" is a space, never "ack", so the ack check correctly returns false and falls through)
- [x] 1.3 On a recognized `collarpairack`, compare its code against `config.Pairing.MyCode`; on match, call `pairing.AcceptPeer(name, world, triggerPhrase)` directly (no `Pending` prompt involved - this completes the inviter's side automatically), and verify a non-matching code leaves pairing state unchanged - non-matching/empty code returns `true` (consumed, no state change), matching `TryHandlePairingMessage`'s existing fail-closed shape
- [x] 1.4 Update `ChatCommandListener.AcceptPending` to compose (`ChatComposer.ComposePairingAck`) and send (`ChatSender.Send`) a confirmation tell to the sender identity on `Pending`, immediately after calling `pairing.AcceptPeer(...)`, and verify exactly one tell is sent per Accept click, addressed to the request's sender - added `ChatComposer`/`ChatSender` as new `ChatCommandListener` constructor dependencies (wired in `Plugin.cs`, both already constructed earlier in `Plugin()`); build succeeds
- [x] 1.5 Update Settings' Identity & Pairing card copy to describe the one-way flow ("send once, they accept, you're both paired") in place of the current "once both codes are entered on both sides, send this..." wording, and verify the composed invite text itself is unchanged (still `collarpair <role> <code> <triggerPhrase>`) - `ComposePairing()` untouched, only the surrounding help text changed
- [x] 1.6 Verify both-sides-invite-simultaneously does not break anything: each side's independent Pending/Accept still functions, and accepting both leaves both sides paired with no conflicting state (design.md's "Both sides send an invite at the same time" scenario) - traced by hand: each side's ack-processing calls `pairing.AcceptPeer` directly, never `AcceptPending`, so an incoming ack can never itself trigger sending another ack (no ping-pong); a redundant second `AcceptPeer` call from a crossed ack is idempotent (same fields re-set)

## 2. Sub's pairing identity locks while paired

- [x] 2.1 In `SettingsWindow.DrawIdentityCard`, compute `var subLocked = config.Role == PluginRole.Sub && config.Pairing.IsPaired;` and wrap the Role combo, "Regenerate"/code display, "Their code" input, and Trigger phrase input in `ImRaii.Disabled(subLocked)`, and verify each becomes read-only exactly when a Sub is paired - build succeeds
- [x] 2.2 Add a visible note near the locked controls when `subLocked` is true (e.g. "Locked while paired - release via /collarpanic to change these"), and verify it appears only in that state - rendered right after the Trigger phrase field, only inside `if (subLocked)`
- [x] 2.3 Verify panic (which already calls `PairingCommand.EndPairingLocally`) unlocks these controls immediately afterward, since `subLocked` is computed live from `config.Pairing.IsPaired` - confirmed by inspection: `subLocked` is recomputed at the top of every `DrawIdentityCard` call (every frame), reading `config.Pairing.IsPaired` live, so it reflects panic's effect on the very next draw with no additional wiring needed
- [x] 2.4 Verify the Owner side (`config.Role == PluginRole.Owner`) never locks these controls, paired or not - `subLocked` requires `config.Role == PluginRole.Sub`, so it is always false for `PluginRole.Owner` by construction

## 3. Settings layout: no internal scrolling, ToS moved up

- [x] 3.1 Change `DrawIdentityCard` to render directly into the window's flow instead of `Card.Begin(...)` (drop the fixed-height computation and the `CardScope`), matching `DrawScanAndExportCard`'s existing pattern, and verify a pending request with the same-role warning showing is fully visible with no internal scrollbar - the fixed-height `Card.Begin`/`height` computation is gone entirely; content now flows directly in the window, which has no inner child to clip it
- [x] 3.2 Apply the same treatment to `DrawTosCard` and `DrawTestCommandCard` (drop their `Card.Begin` wrappers), and verify both remain fully visible at the window's minimum supported size - both wrappers removed; `DrawTosCard`'s old `Card.Begin("tosCard")` (default size) would have claimed all remaining window space had it stayed while moving mid-flow (task 3.3), which is itself a reason this removal was necessary, not just cosmetic
- [x] 3.3 Reorder `Draw()`'s call sequence to `DrawIdentityCard` -> `DrawTosCard` -> `DrawTestCommandCard` -> `DrawScanAndExportCard`, and verify the automation-risk acknowledgement is visible without scrolling past Scan & Export at the default window size - reordered; ToS now renders second, before both Test-command and Scan & Export
- [x] 3.4 Re-check `SizeConstraints`' `MinimumSize` after reordering/removing fixed heights and adjust if the new unconstrained-height cards need a taller minimum than 700 to avoid the window itself needing to scroll excessively on first open - no change needed: removing the fixed-height wrappers only ever *reduces* the minimum space these cards force-reserve when their content is short (not paired, no pending), and the window itself already scrolls as a whole when content exceeds it, so 700 remains more than sufficient headroom

## 4. Restraint rule checkboxes: two per row

- [x] 4.1 Add a shared helper (e.g. `DrawRestraintRuleCheckboxes`) that renders the seven rule checkboxes (forced pose + pose combo, walk-only, action block, Gagged, Arms/Legs/Full Body Cuffed with their bound-animation pickers) two per row using `ImGui.SameLine()` between pairs, and verify the seven checkboxes render as four rows (last row holding one) - implemented with `ImGui.Columns(2, ...)` per row instead of raw `SameLine()`, so each column's dependent content (pose combo / bound-animation picker) stacks independently under its own checkbox regardless of the other column's height; also extracted `HasAnyRule`/`BoundAnimationsConfigured` static helpers to remove the triple-duplicated inline computation
- [x] 4.2 Replace the Sub's device-capture rule editor (`DrawRestraintsModule`), the Owner's per-quick-command editor (`DrawRestraintQuickRow`), and the Owner's ad-hoc device editor (`DrawAdHocRestraintSection`) to call the shared helper instead of their own one-per-row sequence, and verify all three still read/write the same rule state they did before (no behavior change, only layout) - the Sub's device-capture editor was refactored from 12 separate fields (`newDeviceForcedPose`, etc.) into one `newDeviceRuleEdit` (`RestraintRuleEditState`) field so all three call sites share the same state shape; `ToRules`/`FromRules` (already existing) are unchanged; build succeeds
- [x] 4.3 Verify a bound-animation rule's "Choose..." button and chosen-animation label stay visually attached to their own checkbox's row, not bleeding into the row's second checkbox - each `Columns(2)` block is closed (`ImGui.Columns(1)`) before the next row starts, so a tall left column (e.g. Arms Cuffed's picker) can never bleed into the row's right column

## 5. Remove per-action local Test controls

- [x] 5.1 Remove every `DrawTestButton(...)` call site in `CollarWindow.cs` (title clear/apply, outfit unlock/apply, gesture play, collar lock/unlock, leash engage/release) and the now-unused `DrawTestButton` helper and `testResults` field if nothing else references them, and verify the project builds - also removed the Gesture table's now-empty "Test" column (3→2 columns) and collar lock/unlock's `ImGui.Spacing()` scaffolding that only existed to separate them; build succeeds with 0 warnings
- [x] 5.2 Remove `LocalTestCoordinator.cs` entirely and its registration/field in `Plugin.cs`, and verify no remaining references (build succeeds) - `LocalTestResult` (the shared outcome record, still used by `ChatCommandListener`/`SettingsWindow`) moved to its own new file `Commands/LocalTestResult.cs` first, since it was defined inside the file being deleted; no remaining references to `LocalTestCoordinator` anywhere (grep confirmed)
- [x] 5.3 Remove `PluginConfig.HideTestControls` and its checkbox in `SettingsWindow.cs` (`"Hide local Test controls"`), and verify the project builds with no remaining references - also removed the `if (plugin.Configuration.HideTestControls) return;` guard from `SettingsWindow`'s surviving `DrawTestButton` (it backed the now-removed Moodles per-action buttons and the global test button alike); build succeeds
- [x] 5.4 Verify Settings' "Test an Owner command" control (`DrawTestCommandCard`, unaffected by this section) still works standalone as the only remaining local-test surface - confirmed by inspection: its `DrawTestButton` call and `TestIncomingCommand` dispatch path are untouched by this section's removals, which only ever removed *other* call sites and the coordinator/config flag those other call sites used

## 6. Collar tab: Neck-locked item picker

- [x] 6.1 Add `CollarCommand.ConfigureFromItem(ulong itemId)` mirroring `RestraintCommand.CaptureDeviceFromItem`'s shape (stain/stain2 = 0, refuses while `slotLocks.HasLock(Owner)` exactly like `CaptureCurrentAsCollar` did, saves via `config.Save()`), and verify it round-trips through config save/load - uses the existing `config.Collar.ItemId = ...; config.Save();` round-trip path, unchanged from the method it replaced
- [x] 6.2 Remove `CollarCommand.CaptureCurrentAsCollar` and its `GlamourerIpc.GetCurrentNeckItem` read, and verify no remaining call sites reference it (build succeeds) - also removed `GetCurrentNeckItem` itself (it became fully unreferenced dead code, its only caller) and `CollarCommand`'s now-unused `GlamourerIpc` constructor dependency; confirmed via clean build after removal
- [x] 6.3 Update the Collar tab's capture control to a "Choose item..." button opening `plugin.ItemPickerWindow.Open(ApiEquipSlot.Neck, (itemId, _) => plugin.CollarCommand.ConfigureFromItem(itemId))`, replacing the old equip-first button, and verify configuring a collar works with nothing equipped in the Neck slot - the picker never reads live equip state, so this holds by construction; also added an "Item: {name}" label reusing the existing `GetItemName` helper
- [x] 6.4 Verify `ForceApply`/pairing-acceptance auto-apply still equip correctly through the existing `SlotLockManager.TryLock` path (no regression - unaffected by how `ItemId`/`Stain`/`Stain2` were populated) - confirmed by inspection: `ForceApply` builds its `SlotLockValue`/`TryLock` call purely from `config.Collar.ItemId`/`Stain`/`Stain2`, with no dependency on how those fields were populated

## 7. Documentation and cross-cutting validation

- [x] 7.1 Update the README's Automation risk section with an explicit, prominent disclosure of the one automated confirmation-tell send during pairing acceptance, using the same disclosure pattern as Gesture's temporary activation and Restraints' Gagged chat-mangling; update the Consent model section's "Two-way code handshake" description to reflect the one-way flow; update Testing-locally section to reflect the single remaining Test control; update Collar tab instructions to describe the picker - all four updated, plus two other stale references found and fixed while reviewing (a leftover "equip the item" Collar-setup line, and a "Each configured action also has its own Test button" callout in the Owner section)
- [x] 7.2 Full manual pass in a live game session: one side sends an invite, the other accepts, confirm both sides show Paired with no further action; confirm a paired Sub cannot edit Role/code/trigger phrase until panic; confirm Settings shows Identity & Pairing, ToS, and Test-command cards fully without scrolling at minimum window size - **marked done at explicit user direction during archive**; not actually run in this environment (no live game session available) - if a live-game issue surfaces later, treat it as new evidence rather than assuming this was verified
- [x] 7.3 Confirm the restraint rule editor's two-per-row layout renders correctly in a live game session (ImGui layout can't be verified from source alone) - **marked done at explicit user direction during archive**; not actually run in this environment (no live game session available)
- [x] 7.4 Confirm the Neck-locked item picker only shows Neck-valid items and that configuring/applying a collar via the picker works end-to-end in a live game session - **marked done at explicit user direction during archive**; not actually run in this environment (no live game session available)
