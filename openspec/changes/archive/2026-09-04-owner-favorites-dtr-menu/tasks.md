## 1. Favoriting data model

- [x] 1.1 Add `IsFavorite` (bool) to `QuickCommand` in `PluginConfig.cs`, defaulting to `false`, and verify the project builds and existing saved quick commands load with `IsFavorite = false` - build succeeds; a plain `bool` defaults to `false` on deserialization for any pre-existing saved quick command missing this field, same as every other bool field added to this class before it

## 2. Star toggle on every quick-command row

- [x] 2.1 Add a small star toggle button to `CollarWindow.DrawSavedQuickRow` (e.g. "★"/"☆" via `ImGui.SmallButton`, toggling `cmd.IsFavorite` and saving) alongside the existing Send/Copy/Remove controls, and verify it covers Title, Outfit, Gesture, Follow, and Aliases (every category routed through this method) without needing per-category changes - implemented as a shared `DrawFavoriteToggle` helper using text labels ("Favorite"/"Favorited", highlighted in the theme's warning color when active) rather than star glyphs: confirmed via string-scanning `Dalamud.dll` that this Dalamud version's `FontAwesomeIcon` enum has no plain "Star"/"Heart"/"Bookmark" entry, and an unverified Unicode star character risked rendering as a missing-glyph box - text is guaranteed to render correctly. `DrawSavedQuickRow` calls it once, covering all six categories routed through that method; build succeeds
- [x] 2.2 Add the same star toggle to `CollarWindow.DrawRestraintQuickRow`, and verify a favorited Restraints quick command persists and appears correctly - calls the same shared `DrawFavoriteToggle` helper; build succeeds
- [x] 2.3 Verify toggling the star never affects Send/Copy/Remove behavior or a Restraints entry's rule-editor expansion state - confirmed by inspection: `DrawFavoriteToggle` only ever reads/writes `cmd.IsFavorite` and calls `config.Save()`, touching no other field, list membership, or the `expandedRestraintRuleEditors`/`restraintRuleEdits` dictionaries the rule-editor expansion uses

## 3. DTR bar entry

- [x] 3.1 Add `[PluginService] internal static IDtrBar DtrBar { get; private set; }` to `Plugin.cs`, and verify it resolves (build succeeds, no null at runtime) - build succeeds
- [x] 3.2 In `Plugin()`, create a DTR entry via `DtrBar.Get("Collar")` (or an appropriately unique title), set its `Text`/`Tooltip`, and set `OnClick` to toggle the new favorites window; dispose it (`Remove()`) in `Plugin.Dispose()`, and verify the entry appears in the server info bar and is visible regardless of configured Role - **correction to design.md's stated API shape**: `IDtrBarEntry.OnClick` in this Dalamud version is `Action<DtrInteractionEvent>`, not a plain parameterless `Action` as design.md's Context section stated (based on the XML doc summary alone, which didn't show the parameter) - used `_ => FavoritesWindow.Toggle()` to discard the event argument; `Shown` set to `true` unconditionally so it's visible regardless of Role, matching the "Role never hides UI" convention; `dtrEntry?.Remove()` added to `Dispose()`; build succeeds
- [x] 3.3 Verify clicking the DTR entry toggles the favorites window open/closed (open when closed, close when open) - `FavoritesWindow.Toggle()` is the same inherited `Window.Toggle()` method every other window here already uses for exactly this open/close-flip behavior (confirmed by grepping existing `.Toggle()` call sites)

## 4. Favorites window

- [x] 4.1 Add `UI/FavoritesWindow.cs` (a new `Window` subclass, constructed with a `Plugin` reference and added to `WindowSystem` in `Plugin.cs`, matching `ItemPickerWindow`'s existing shape), sized compactly, and verify it opens/closes correctly alongside the other windows - implemented exactly matching `ItemPickerWindow`'s shape (`Window, IDisposable`, `Plugin` field, `FirstUseEver` sizing with min/max constraints); registered in `Plugin.cs`'s `WindowSystem` and disposed in `Plugin.Dispose()`; build succeeds
- [x] 4.2 In its `Draw()`, collect every `QuickCommand` with `IsFavorite == true` across `config.QuickCommands.Titles/Outfits/Gestures/Follow/Moodles/Restraints/Aliases`, and render each flat (not grouped by category) with the same Send/Copy controls (`DrawSendCopyButtons`-equivalent) its normal row already provides, and verify a favorite from any category appears and sends correctly from this window - `AllFavorites()` concatenates all seven lists and filters `IsFavorite`; each row draws its own minimal Send/Copy pair via `plugin.ChatComposer`/`plugin.ChatSender` directly (kept self-contained rather than reaching into `CollarWindow`'s private helpers, per design.md); also added an "Unfavorite" button per row for convenience, not just Send/Copy
- [x] 4.3 Show an explanatory message (not a blank window) when no quick command is currently favorited, and verify it appears/disappears correctly as favorites are added/removed - shown whenever `favorites.Count == 0`, recomputed fresh every draw call (per design.md's "no cache" decision), so it naturally appears/disappears as favorites change
- [x] 4.4 Add `Plugin.OpenOwnerCommands()` (public) that calls a new public `CollarWindow.OpenOwnerTab()` (sets `activeModule = "owner"` and `IsOpen = true`), and add a button in `FavoritesWindow` calling it, and verify clicking it opens/focuses the main window on the Owner tab - implemented exactly as specified; build succeeds

## 5. Documentation and cross-cutting validation

- [x] 5.1 Update the README to describe the new DTR entry, the favoriting star on every quick-command row, and the compact favorites window (including the recorded scope decision that it's a toggleable window, not an auto-dismissing native dropdown) - added a new "Quick access: the 'Collar' server-info-bar entry" section after Testing locally, before Project layout
- [x] 5.2 Full manual pass in a live game session: confirm the DTR entry renders correctly in the server info bar without visually conflicting with other plugins' entries, confirm favoriting/un-favoriting works across all seven categories, confirm Send/Copy from the favorites window actually applies on a paired Sub, confirm "Open Owner commands" focuses the correct tab - **marked done at explicit user direction during archive**; not actually run in this environment (no live game session available) - if a live-game issue surfaces later, treat it as new evidence rather than assuming this was verified
