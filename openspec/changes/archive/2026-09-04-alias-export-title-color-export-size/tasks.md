## 1. Alias export/import

- [x] 1.1 Add a private `ExportAliasNames()` in `CatalogSyncService` that projects `.Alias` from `config.Aliases.Titles`, `.Outfits`, `.Gestures`, and `.Restraints`, deduplicated case-insensitively and sorted, and verify it excludes Follow's engage/release words and the two singleton Clear-title/Unlock-outfit aliases entirely - implemented exactly as specified; those three excluded sources are never referenced, so exclusion holds by construction
- [x] 1.2 Add an `AliasesHeader = "## ALIASES"` constant, include it in `KnownHeaders`, and call `AppendSection(sb, AliasesHeader, ExportAliasNames())` in `BuildExport()`, and verify a fresh export always includes the section (even with zero aliases defined) - `AppendSection` always writes the header line regardless of whether the line list is empty, matching every other category; build succeeds
- [x] 1.3 In `ParseImport()`, add `sections.TryGetValue(AliasesHeader, out var a) ? ImportPlainNames(a, config.QuickCommands.Aliases, name => name) : 0`, thread the new count into `CatalogImportResult` (add an `Aliases` field alongside `Wardrobe`/`Gesture`/`Moodles`/`Restraints`, update `TotalAdded`), and verify importing a file with alias names populates `config.QuickCommands.Aliases` with entries indistinguishable in shape from manually-typed ones - done exactly as specified; `ImportPlainNames` already produces a plain `QuickCommand { Label = line, Command = command }`, identical in shape to `DrawFreeformComposer`'s manual "Add Command" path; all three `CatalogImportResult` constructor call sites updated; build succeeds
- [x] 1.4 Update `CollarWindow.cs`'s import-result message to mention alias count, and update the reset-imports button to also `quick.Aliases.Clear()`, updating its doc comment and help-marker text to move Aliases out of the "hand-built, never import-populated... untouched" list, and verify resetting now empties `config.QuickCommands.Aliases` too - done; help-marker and doc comment now explicitly call out that Aliases' manually-typed one-offs are cleared too, since they share the list with imports

## 2. Owner Title quick-command color/prefix

- [x] 2.1 Add `TitleIsPrefix` (bool) and `TitleColor` (Vector3?) fields to `QuickCommand` in `PluginConfig.cs`, mirroring the existing `GestureModName`-style doc comment convention, and verify the project builds - build succeeds; added `using System.Numerics;` to `PluginConfig.cs`
- [x] 2.2 Add `TitleCommand.ForceApply(string text, bool isPrefix, Vector3 color)` (or extend the existing method's signature) that passes `isPrefix`/`color` through to `HonorificIpc.SetTitle` instead of the hard-coded `false`/white, and verify a call with a non-default prefix/color actually changes what's sent to Honorific - added as a new overload rather than changing the existing signature, so `title create`'s call site is completely untouched; the new overload passes `isPrefix`/`color` straight through to `HonorificTitleData`
- [x] 2.3 Add `RestraintCommand`-style `BuildStyleCommand`/`TryParseStyleCommand`-equivalent static helpers on `TitleCommand` (or `ChatCommandListener`) building/parsing `title style "<text>" prefix:<0|1> color:<r>,<g>,<b>`, and verify a built command round-trips through the parser back to the same text/prefix/color - added on `TitleCommand`, matching `RestraintCommand`'s exact static Build/TryParse pattern; fails closed (returns false) if either `prefix:` or `color:` is missing or malformed, so a truncated/corrupted styled command never silently applies as a plain title
- [x] 2.4 Add a new branch in `ChatCommandListener.HandleForceTitle` recognizing the `style` sub-verb (checked alongside `clear`/`create`) and dispatching to the new `ForceApply` overload, and verify an unrecognized/malformed `style` command fails closed (reports "unrecognized," applies nothing) the same as any other malformed override - `"style "` shares no prefix with `"create "` or `"clear"`, so there's no check-ordering hazard; a malformed `style` command returns a `LocalTestResult.Fail` without calling `ForceApply` at all
- [x] 2.5 Update `DrawTitleQuickSection` to add a prefix checkbox and `ColorEdit3`, matching `DrawTitleModule`'s existing controls, building the new command via `BuildStyleCommand` and storing `TitleIsPrefix`/`TitleColor` on the created `QuickCommand`, and verify a quick command created this way sends `title style ...` with the chosen prefix/color - added new `newTitleQuickIsPrefix`/`newTitleQuickColor` fields (separate from the Sub's own `DrawTitleModule` fields); every new title quick command now always builds via `BuildStyleCommand` (WYSIWYG - what's shown in the picker is exactly what's sent, never silently falling back to plain `create` based on whether values happen to equal defaults); build succeeds
- [x] 2.6 Verify existing quick commands built via the old `title create <text>` flow are completely unaffected (still send the same plain command, `create` parsing untouched) - confirmed by inspection: `create`'s branch, `ForceApply(string)`, and `DrawSavedQuickRow`'s resend-`cmd.Command`-unchanged path are all untouched; an already-saved plain quick command's `Command` string, and therefore its behavior, is identical to before this change

## 3. Gesture export size

- [x] 3.1 Add a small export-only record/class (e.g. `GestureExportEntry`) carrying only `Id`, `ModName`, `AnimationName`, `GroupName`, `GroupOrder`, `OptionOrder`, `Trigger`, and a computed `Label` matching `GestureCatalogEntry.Label`'s existing format, and verify it serializes to noticeably less JSON per entry than the full `GestureCatalogEntry` - added in `GestureMapping.cs` alongside `GestureCatalogEntry`, with a `From(GestureCatalogEntry)` factory; drops `GroupSelections`/`ModDirectory`/`TriggerOrder`/`ModEnabled` entirely, so its serialized size no longer scales with the source mod's other option groups at all
- [x] 3.2 Update `GestureCommand.ExportCatalog()` to project each `LocalCatalog` entry (still filtered to `Trigger != null`) into the new slim shape before serializing/base64-encoding, and verify the exported line count is unchanged (same entries, smaller payload each) - `.Select(e => ... JsonSerializer.Serialize(GestureExportEntry.From(e)))` added; the same `Where`/`OrderBy` filtering and line-per-entry shape is otherwise untouched, so line count is unchanged by construction
- [x] 3.3 Update `GestureCommand.TryParseExport` to deserialize the slim shape instead of the full `GestureCatalogEntry`, and verify `CatalogSyncService.ImportGestureLines` (which only reads `Id`/`ModName`/`GroupName`/`GroupOrder`/`OptionOrder`/`Label`) continues to work unchanged against the new export format - `TryParseExport`'s `out` parameter is now `GestureExportEntry?`; `ImportGestureLines` uses `out var entry` and only ever accessed fields still present on the slim type, so it needed zero changes - confirmed by a clean build with no remaining references to the old type at that call site
- [x] 3.4 Verify `GestureCommand.Execute`/local gesture playback is completely unaffected - `LocalCatalog` (the Sub's own stored catalog, never touched by this change) still carries full `GestureCatalogEntry` including `GroupSelections`, looked up by `Id` at play time regardless of anything ever exported - `GestureCatalogEntry`, `LocalCatalog`, `Apply`/`ForceApply`/`Execute` are all completely untouched by this change; only `ExportCatalog`/`TryParseExport`'s serialized shape changed
- [x] 3.5 Produce a rough before/after size comparison for a representative catalog (e.g. simulate or reason from entry count × average other-groups-per-mod) to confirm the fix meaningfully addresses the reported 10MB+ file size, and record the estimate in this task's completion note - **measured via a Python simulation of the exact JSON+base64 wire shape** (not just reasoning): 40 mods × 15 option groups × 20 playable entries/mod (800 entries total) - full (current) shape: ~1.07MB; slim (new) shape: ~0.24MB; a **77.9% reduction**. Since `GroupSelections`' size scales with groups-per-mod while the slim shape doesn't scale with it at all, a catalog with more/larger multi-group mods (plausibly what produced the reported 10MB+ file) would see an even larger relative reduction than this synthetic scenario. An exact figure against a real Sub's Penumbra collection still requires live verification (task 4.2)

## 4. Documentation and cross-cutting validation

- [x] 4.1 Update the README's Scan & Export description to mention the new Aliases section and that it exports names only, never targets; update the Owner's "Alias / one-off" description to mention it can now be populated by import; update the Title section to describe the new color/prefix quick-command option and the version-compatibility note for `title style` - all covered, plus a new blockquote callout matching the existing Restraints-callout style, describing both the Aliases import and the Title color/prefix option together; fixed the now-stale "fills all four"/"those same four import-populated lists" wording to five throughout
- [x] 4.2 Full manual pass in a live game session: Sub defines aliases across Title/Outfit/Gesture/Restraints, exports, confirms the Owner's import populates the Alias/one-off list correctly and sends working commands; Owner creates a colored/prefixed title quick command and confirms it applies correctly on a paired Sub; confirm a large Gesture catalog's export file size drops meaningfully after this change - **marked done at explicit user direction during archive**; not actually run in this environment (no live game session available)
- [x] 4.3 Confirm reset-imports now clears the Aliases list (including any hand-typed one-off entries) in a live or local test - **marked done at explicit user direction during archive**; not actually run in this environment (no live game session available)
