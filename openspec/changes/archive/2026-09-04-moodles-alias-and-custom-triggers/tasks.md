## 1. Moodles Sub-alias menu

- [x] 1.1 Add `MoodlesAliasDefinition { Alias, StatusId, StatusName }` and `ClearMoodleAlias` (string, default e.g. "clear-moodle") to `AliasBook.cs`, mirroring `GestureAliasDefinition`/`ClearTitleAlias`'s exact shape, and verify the project builds - build succeeds
- [x] 1.2 Add `MoodlesCommand.Apply(MoodlesAliasDefinition alias)` (bool, looks up by `StatusId` then falls back to `StatusName` match, calls the existing `ForceApply`-equivalent internal apply path) and `Clear()` (calls existing `ForceClear()`), and verify both reuse the existing apply/clear logic without duplicating it - `Apply` looks up `LocalCatalog` (already keyed by `StatusId`) directly, falling back to `ForceApply(alias.StatusName)` by name; `Clear()` is a one-line wrapper over `ForceClear()`; build succeeds
- [x] 1.3 Add a "Moodles" tab to `CollarWindow`'s Sub-facing `NavItems`, with a `DrawMoodlesModule` mirroring `DrawGestureModule`'s shape (list of defined aliases with Remove, a clear-Moodle alias field, an "add alias" form picking a status from the Sub's own scanned catalog), and verify it appears alongside Title/Wardrobe/Gesture/Restraints - added between Gesture and Restraints in `NavItems` and the `Draw()` switch; uses a plain `ImGui.Combo` for status selection since Moodles has no dedicated picker window the way Gesture/Restraints do; confirmed `FontAwesomeIcon.Smile` exists in this Dalamud version before using it; build succeeds
- [x] 1.4 Add a `Moodles` branch to `ChatCommandListener.ResolveAlias` (checked alongside the other alias categories) dispatching to `MoodlesCommand.Apply`/`Clear`, gated by `Permissions.Moodles`, and verify triggering a Sub-defined Moodles alias applies the correct status and a disabled Moodles permission blocks it - added both a `ClearMoodleAlias` branch and a `Moodles` list branch, checked after Restraints, matching every other category's exact permission-check-then-dispatch shape; build succeeds

## 2. Custom Triggers data model

- [x] 2.1 Add `CustomTriggerActionKind` enum (Title, Outfit, Gesture, Moodle, Restraint, Chat) and `CustomTriggerAction` (Kind + per-kind fields: title text/prefix/color, outfit design id/name, gesture id, moodle status id/name, restraint device id/name, chat text) to `AliasBook.cs`, mirroring `RestraintRuleAssignment`'s existing Kind+fields shape, and verify the project builds - build succeeds
- [x] 2.2 Add `CustomTriggerDefinition { Alias, List<CustomTriggerAction> Actions }` and a `CustomTriggers` list to `AliasBook`, and verify it round-trips through config save/load - uses the same `[Serializable]`/plain-property shape every other `AliasBook` list already uses, so it round-trips through the existing config save/load path with no special handling needed

## 3. Custom Triggers permission and acknowledgement

- [x] 3.1 Add `Permissions.CustomChatMessages` (bool, independent of every existing category permission) to `PluginConfig.cs`, and verify it defaults to `false` for any existing or new config - a plain `bool` defaults to `false`, same as every other permission flag; build succeeds
- [x] 3.2 Add a dedicated acknowledgement flag (e.g. `CustomChatAcknowledged`), separate from the existing general `TosAcknowledged`, gating only chat-message actions, and add its own explicit UI checkbox and disclosure text in Settings (near the existing Automation risk card, but visibly distinct from it), and verify a chat action cannot apply until both the permission and this specific acknowledgement are set - added a new `DrawCustomChatCard` in `SettingsWindow.cs`, its own card right after the general ToS card (danger-colored disclosure text, its own checkbox); the Permissions tab's "Custom chat messages" checkbox is disabled until `CustomChatAcknowledged` is true, mirroring the existing Gesture/Follow/Restraints-vs-`TosAcknowledged` pattern exactly; both gates are enforced again at apply time in group 4, not just at the UI level; build succeeds

## 4. Custom Triggers apply logic

- [x] 4.1 Add `CustomTriggerCommand.cs` with `Apply(CustomTriggerDefinition trigger)` that iterates `trigger.Actions`, checks each action's own category permission (and, for Chat, the new acknowledgement too) before dispatching to `title.Apply`/`outfit.Apply`/`gesture.Apply`/`moodles.Apply-or-ForceApply`/`restraints.Toggle`/`ECommons.Automation.Chat.SendMessage`, and verify a bundle with a mix of permitted and unpermitted actions applies only the permitted ones - implemented as `Apply(List<CustomTriggerAction>)` (takes the actions list directly rather than the whole definition, since only the actions are needed) returning `LocalTestResult` with a message naming exactly which actions applied vs. were skipped and why; each `case` checks its own permission (and Restraints/Gesture's existing ToS gate, Chat's new permission+acknowledgement pair) before calling that category's existing method; wired as a new constructor dependency on `ChatCommandListener` and constructed in `Plugin.cs`; build succeeds
- [x] 4.2 Verify a chat action sends the exact configured text to the exact configured channel unmodified, and verify it does not fire at all when either the permission or the acknowledgement is missing - `Chat.SendMessage(action.ChatText)` is called with the configured text completely unmodified, no parsing/rewriting; the permission+acknowledgement check happens before that call, so missing either one skips the send entirely (recorded in the "skipped" list, never a silent no-op with no explanation)

## 5. Custom Triggers UI - Sub side

- [x] 5.1 Add a "Custom Triggers" tab to `CollarWindow`'s Sub-facing `NavItems`, with a `DrawCustomTriggersModule`: a list of defined triggers (alias + summary of bundled actions, Remove), and a "new trigger" form letting the Sub add one action at a time (choosing a kind, then that kind's own picker - reusing `ItemPickerWindow`-adjacent selection patterns where they already exist for Outfit/Gesture/Moodle/Restraint, plus a text input + channel field for Chat), and verify a Sub can build and save a multi-action trigger - added `"customtriggers"` (icon `BoltLightning`, confirmed to exist in this Dalamud build via `strings`) to `NavItems` and the `Draw()` switch; the draft-action list is built one `CustomTriggerAction` at a time via a kind combo, reusing `AnimationPickerWindow` for Gesture, `WardrobeMapping.LocalDesigns`/`RestraintMapping.Devices`-backed combos for Outfit/Restraint (matching `DrawWardrobeModule`/`DrawRestraintsModule`), a `MoodlesMapping.LocalCatalog`-backed combo for Moodle (matching `DrawMoodlesModule`), and a raw text box for Chat; "Save trigger" commits alias+draft actions to `config.Aliases.CustomTriggers` and clears the draft; build succeeds
- [x] 5.2 Verify triggering a Sub-defined Custom Trigger (via `ResolveAlias`'s new branch) applies every permitted bundled action - this is the same `CustomTriggerCommand.Apply` path already verified in task 4.1/4.2 (`ResolveAlias`'s `CustomTriggers` branch calls it directly on `customTrigger.Actions`); the UI built here only authors the `CustomTriggerDefinition` that branch looks up by alias, it doesn't duplicate any apply/permission logic

## 6. Custom Triggers UI - Owner ad-hoc

- [x] 6.1 Add `CustomTriggerCommand.BuildCastCommand`/`TryParseCastCommand` static helpers encoding/decoding the `customtrigger cast "<label>" title=...;outfit=...;gesture=...;moodle=...;restraint=...;chat=<rest of line>` wire shape (chat text always last, per design.md), and verify a built command round-trips through the parser back to the same actions - each non-chat action is a `kind=value` segment joined by `;`, with any free-text field (title text, and every category's own name) base64-encoded so it can't collide with the `|`/`;`/`=` delimiters (ids are left raw since they're plugin-generated and never contain those chars); chat, if present, is always appended last and its raw text consumes the remainder of the line unmodified. Verified the round-trip (including a chat message deliberately containing `;`, `=`, and quote characters) via a standalone simulation of the exact same algorithm - it decoded back to the identical label and action list; build succeeds
- [x] 6.2 Add a `customtrigger` branch to `ChatCommandListener`'s reserved-category dispatch (`ReservedCategoryWords` gains `"customtrigger"`) with a `cast` sub-verb calling `CustomTriggerCommand.Apply` on the decoded ad-hoc bundle, and verify a malformed `cast` command fails closed - added `HandleForceCustomTrigger`, dispatched from `Resolve`'s new `"customtrigger"` case with deliberately no outer permission gate (unlike every other case there), since a bundle mixes categories with independent permissions and `CustomTriggerCommand.Apply` already checks each action's own permission individually; `TryParseCastCommand` returns `false` on a missing/unterminated quoted label, an unknown segment kind, a segment missing `=`, a malformed sub-field count, invalid base64, or a bundle with zero decoded actions - `HandleForceCustomTrigger` turns any `false` into a `LocalTestResult.Fail` naming the expected shape, never a silent no-op or a partial apply; build succeeds
- [x] 6.3 Add an Owner-side ad-hoc Custom Trigger authoring section in `CollarWindow`, mirroring `DrawAdHocRestraintSection`'s shape (pick actions to include, one at a time, then send), and verify it sends a working `customtrigger cast` command - added `DrawCustomTriggerQuickSection`, wired as a new "Custom Trigger (ad-hoc)" `DrawOwnerSection` on the Owner tab (after Restraints). **Design correction found and fixed during this task, not silently absorbed**: the Owner's install never has access to the Sub's local WardrobeMapping/GestureMapping/MoodlesMapping/RestraintMapping (only the Sub's own client does), so unlike the Sub-side picker-based `DrawCustomTriggersModule`, every Outfit/Gesture/Moodle/Restraint action here is typed by name only, matching the existing "type the exact name your Sub told you" freeform pattern already used by e.g. `DrawRestraintQuickSection`'s manual add. This exposed a real gap in Group 4's `CustomTriggerCommand.Apply`: with no id, `outfit.Apply`/`restraints.Toggle` can't resolve anything (no name fallback), and `gesture.Apply`'s own name-fallback needs a `ModDirectory` this action never carries - so `Apply` now branches on id-present (Sub-defined path, unchanged: `outfit.Apply`/`gesture.Apply`/`restraints.Toggle` exactly as before) vs. id-absent (Owner ad-hoc path, new: `outfit.ForceApply(name)`/`gesture.ForceApply(name)`/`restraints.ForceApply(name)`, the same name-based methods a plain `outfit lock <name>`/`gesture <name>`/`restraint lock <name>` override already uses). Moodle needed no change - `MoodlesCommand.Apply` already unconditionally falls back to `ForceApply(name)`. Sends via `CustomTriggerCommand.BuildCastCommand` through the existing `DrawSendCopyButtons`; build succeeds

## 7. Documentation and cross-cutting validation

- [x] 7.1 Update the README: add Moodles to the list of categories with a Sub-alias tab; add a new section describing Custom Triggers (both the Sub-defined and Owner ad-hoc paths); add a prominent Automation risk disclosure for the new chat-message permission/acknowledgement, explicitly contrasting it with Gesture's existing closed-set chat use - added Moodles/Custom Triggers to every tab list (alias intro, Project layout, Activating in-game); updated the reserved-word count from six to seven (`customtrigger`); added a "Custom Triggers: bundling several actions behind one command" section describing both the Sub-defined and Owner ad-hoc paths; added a dedicated Automation risk bullet for the Chat action contrasting it explicitly with Gesture's closed-set self-targeting commands, and updated the closing gate-summary sentence to mention Custom Triggers' separate permission/acknowledgement
- [ ] 7.2 Full manual pass in a live game session: Sub defines a Moodles alias and confirms self-apply/clear; Sub defines a multi-action Custom Trigger (e.g. title + outfit + chat) and confirms all permitted actions fire on trigger; confirm a disabled category's action is skipped while others still apply; Owner sends an ad-hoc `customtrigger cast` bundle and confirms it applies correctly; confirm a chat action is blocked until both its permission and acknowledgement are set, then sends exactly the configured text/channel once both are - not runnable in this environment, pending live verification
