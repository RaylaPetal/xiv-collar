## 1. Fix the pre-existing send-gate bug

- [x] 1.1 Change `CollarWindow.cs`'s `canSend` computation to `plugin.Configuration.Pairing.IsPaired` in place of the raw `PeerName`/`PeerWorld` null checks, and verify a side that has panicked (Paired=false, PeerName/World still cached) now shows Send disabled - build succeeds; also updated the "No /tell target yet" warning text to mention the post-panic case
- [x] 1.2 Change `ChatComposer.Wrap`'s target-addressing check to `pairing.IsPaired` in place of the raw `PeerName`/`PeerWorld` null checks, and verify a composed command after panic carries no `/tell` prefix, the same as before any pairing ever existed - `!pairing.IsPaired` now returns the bare trigger+body with no `/tell` prefix whenever Paired is false, regardless of cached PeerName/World
- [x] 1.3 Verify an actively paired side's compose/send behavior is unchanged (regression check) - confirmed by inspection: `AcceptPeer` always sets `Paired`, `PeerName`, and `PeerWorld` together, so `IsPaired` and the old presence-only check agree on every actively-paired state; they diverge only in the post-panic case this fix targets

## 2. Panic sends a best-effort notification

- [x] 2.1 Add `ChatComposer.ComposeUnpairNotice(string name, string world, string role)` building `/tell {name}@{world} collarunpair {role}`, and verify it composes a well-formed `/tell` - implemented as `ComposeUnpairNotice(name, world)` computing the role token internally from `config.Role`, matching `ComposePairingAck`'s exact existing pattern rather than taking role as a parameter
- [x] 2.2 Update `PanicHandler.Panic()` to capture `config.Pairing.PeerName`/`PeerWorld` into local variables before any other step runs, and add a new first `RunStep` that composes (`ChatComposer.ComposeUnpairNotice`) and sends (`ChatSender.Send`) the notification when a peer was cached, isolated in its own try/catch so a send failure never affects any other panic step - `PanicHandler` needs `ChatComposer`/`ChatSender` as new constructor dependencies, wired in `Plugin.cs` - done exactly as specified; build succeeds
- [x] 2.3 Verify panic's own local effects (unpair, revert, release locks, clear title, etc.) all still complete when the notification send throws or when no peer was cached to notify - the notify step is wrapped in the same `RunStep` try/catch as every other step, and is a no-op (no send attempted) when `peerName`/`peerWorld` are null/blank, so neither case can affect the steps that follow

## 3. Receiving side recognizes the notification

- [x] 3.1 Add a `PeerUnpairedNotice` (role, timestamp or similar) property plus a `PeerUnpairedNoticeChanged` event on `ChatCommandListener`, mirroring `Pending`/`PendingChanged`'s existing shape - implemented as a `readonly record struct PeerUnpairedNotice(PluginRole PeerRole)` (no timestamp needed - the header clears it explicitly via a new `DismissPeerUnpairedNotice()` rather than auto-expiring); build succeeds
- [x] 3.2 Add a `TryHandlePanicNoticeMessage` path in `ChatCommandListener.OnChatMessage`, checked alongside the existing `collarpair`/`collarpairack` checks, that recognizes the `collarunpair` keyword and, only if the sender's name/world matches `config.Pairing.PeerName`/`PeerWorld`, sets `PeerUnpairedNotice` and raises the event, and verify a `collarunpair` tell from a sender that does not match the configured peer is ignored - implemented as `TryHandleUnpairNoticeMessage`, matching the sender the same way (name required, world checked only if extractable) as the existing ongoing-trigger-tell peer check just below it in `OnChatMessage`, for consistency; a non-matching sender returns `true` (consumed, no state change) per the same fail-closed shape every other handshake message here uses
- [x] 3.3 Verify an unrecognized-keyword tell still falls through to alias parsing unaffected - `collarunpair` shares no prefix with `collarpair`/`collarpairack` (`"collarunpair".StartsWith("collarpair")` is false), so there is no check-ordering hazard and a genuine alias tell is unaffected

## 4. Header surfaces the notice

- [x] 4.1 In `CollarWindow.DrawCharacterHeader`, when `PeerUnpairedNotice` is set and `config.Role == PluginRole.Owner`, show an explanation that the Sub's side ended via panic and surface the existing "Release pairing" button (calling the existing `PairingCommand.ReleasePeer()`, unchanged) alongside it; clear the notice once the Owner clicks it, and verify the button still works exactly as it does today - this branch is checked before the normal `!pairing.IsPaired`/Owner branches (an Owner's own `Paired` flag is untouched by their Sub panicking, so it would otherwise fall into the ordinary "Owns: X" branch and never show); clicking the button calls the same unchanged `ReleasePeer()` plus the new `DismissPeerUnpairedNotice()`; build succeeds
- [x] 4.2 When `PeerUnpairedNotice` is set and `config.Role == PluginRole.Sub`, show an informational note that the Owner's side ended via panic, with no new button and no change to the Sub's own pairing state or available actions, and verify no new unpair path is reachable from this notice - added as an extra line under the existing "Owned by: X / Locked until panic" text (which is otherwise completely unchanged), plus a "Dismiss" button that only clears the transient display flag via `DismissPeerUnpairedNotice()` - it calls no `PairingCommand` method and changes no pairing state, so it introduces no new *unpairing* capability, reconciling this task's "no new button" wording with design.md's explicit "Sub dismisses the note"; the Sub's actual pairing state and every other available action are untouched
- [x] 4.3 Verify the notice display doesn't conflict with the existing Pending-request display (a fresh pairing request arriving should still take precedence/display sensibly if both happen to be present) - the `pending is { } request` branch is checked first in the `if`/`else if` chain, so a fresh Pending always takes visual precedence over any stale notice; additionally, `AcceptPending()` now explicitly clears `PeerUnpairedNotice` on completion so a freshly re-established pairing never keeps showing a stale "they panicked" banner afterward

## 5. Documentation and cross-cutting validation

- [x] 5.1 Update the README's Automation risk section to list the panic notification as the second narrow exception to "no automated sending," alongside the existing pairing-confirmation-tell disclosure; update the Consent model section to mention the best-effort peer notification and its explicit non-guarantee - restructured the Automation risk bullet into "Two narrow, explicit exceptions" covering both by name; updated the Consent model's handshake bullet and the Panic bullet; also fixed a now-stale "the one narrow, explicit exception" (singular) left over from before this change
- [ ] 5.2 Full manual pass in a live game session: Sub panics while paired, confirm the Owner sees the notice and "Release pairing," confirm the Owner's Send buttons were already disabled by the panic itself (task 1) even before clicking Release; Owner panics while paired, confirm the Sub sees the informational note with no new button - not runnable in this environment, pending live verification
- [ ] 5.3 Confirm a `collarunpair` tell from an unrecognized sender (not the configured peer) produces no notice and no state change, in a live game session or via the local test tool - not runnable in this environment, pending live verification
