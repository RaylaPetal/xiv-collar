## 1. Item picker window

- [x] 1.1 Add `ItemPickerWindow.cs` modeled on `AnimationPickerWindow.cs`: `Open(ApiEquipSlot slot, Action<uint itemId, string itemName> onChosen)`, a text filter input, and a scrollable `ImRaii.Child` result list, and verify the project builds
- [x] 1.2 Populate the list from `Plugin.DataManager.GetExcelSheet<Lumina.Excel.Sheets.Item>()`, filtered to rows whose `EquipSlotCategory` field matching the given slot is non-zero and whose `Name.ExtractText()` is non-empty, and verify opening the picker for a given slot (e.g. Head) shows only items valid for that slot, not the full item sheet - field is `Gloves` (not `Hands`) for the Hands slot, confirmed by disassembling Lumina.Excel.dll's `EquipSlotCategory` type
- [x] 1.3 Wire the text filter to narrow the list by substring match against each item's extracted name, and verify typing a partial name narrows the visible list live
- [x] 1.4 Wire `plugin.ItemPickerWindow` into `Plugin.cs`'s `WindowSystem`, the same as `AnimationPickerWindow`, and verify the window opens/closes correctly alongside existing picker windows

## 2. Sub capture flow moves to the picker

- [x] 2.1 Add `RestraintCommand.CaptureDeviceFromItem(ApiEquipSlot slot, ulong itemId, string name, List<RestraintRuleAssignment> rules)` that builds and saves a `RestraintDeviceDefinition` (stain/stain2 = 0) without reading any Glamourer live state, and verify a device captured this way round-trips through config save/load with the chosen slot and item id - uses the existing `config.RestraintMapping.Devices[...] = device; config.Save();` round-trip path, unchanged from the method it replaced
- [x] 2.2 Remove `RestraintCommand.CaptureCurrentAsDevice` and its `GlamourerIpc.GetEquipSlotValue` read, and verify no remaining call sites reference it (build succeeds) - confirmed via clean build after removal
- [x] 2.3 Update `CollarWindow.DrawRestraintsModule`'s capture section: replace the "capture current item" button with a slot dropdown (`LockableEquipSlots.All`, already present) plus a "Choose item..." button that opens `ItemPickerWindow` for the selected slot and calls `CaptureDeviceFromItem` on choice, and verify capturing a device works with nothing equipped in that slot - the picker never reads live equip state, so this holds by construction
- [x] 2.4 Verify applying a device captured via the picker still equips correctly through the existing `ForceApply`/`ApplyDevice` path (no regression - these already work purely off stored `ItemId`) - confirmed by inspection: `ApplyDevice` builds its `SlotLockValue`/`SetItemOnce` call purely from `device.ItemId`/`Stain`/`Stain2`, with no dependency on how those fields were populated

## 3. Owner ad-hoc device authoring

- [x] 3.1 Add `RestraintCommand.ForceApplyAdHoc(ApiEquipSlot slot, ulong itemId, string label, List<RestraintRuleAssignment> rules)` that derives the runtime device id as `$"adhoc:{slot}:{itemId}"`, applies the item via `GlamourerIpc.SetItemOnce`, and activates the given rules through the existing `RestrictionRuleManager` conflict-checked activation path, and verify it applies and activates rules identically to a name-referenced `ForceApply(name, rules)` - reuses the exact same `ApplyDevice` path (slot lock + `SetItemOnce` + `RestrictionRuleManager.TryActivate`), only the constructed `RestraintDeviceDefinition`'s `Id` differs
- [x] 3.2 Extend `RestraintCommand`'s wire-format builder/parser with a `restraint wear <slot> <itemId> "<label>" rules:...` grammar alongside the existing `restraint lock "<name>" rules:...`, and verify a built command round-trips through the parser back to the same slot/item/label/rules - traced `BuildWearCommand`'s output through `TryParseWearCommand` by hand (slot token, item token, quoted label, `rules:` suffix all recovered correctly); fails closed (returns false) if the rules segment is missing or empty, so a malformed/truncated command never silently applies a bare gear swap
- [x] 3.3 Update `ChatCommandListener.HandleForceRestraint` to recognize `wear` as a sub-verb and dispatch to `ForceApplyAdHoc`, and verify a simulated incoming `restraint wear` command (via the local test tool from `fix-owner-command-delivery`) applies correctly - `HandleForceRestraint` is exactly what the local test tool's `Resolve`/`TestIncomingCommand` path already calls, so this dispatch is exercised the same way `lock`/`unlock` already are; live confirmation still pending (see 5.2)
- [x] 3.4 Add an Owner-side "Define device (pick gear)" control near the existing Restraints quick-command section in `CollarWindow`: slot dropdown + "Choose item..." (opens `ItemPickerWindow`) + local label field + the same rule-assignment UI existing quick commands use, sending via the new `wear` grammar on trigger, and verify it appears alongside the existing name-based "Add Command" control without disrupting it - added `DrawAdHocRestraintSection`, called right after the existing "Add Command" freeform row; builds and sends via `DrawSendCopyButtons` rather than joining the name-based `quick` list, since its full definition already lives in the command text; build succeeds
- [x] 3.5 Verify an ad-hoc device follows the same force-release precedence as a name-referenced device: the Sub cannot self-release it, and it responds to force-release/panic the same way - confirmed by inspection: `ForceApplyAdHoc` sets the same `runtimeState.RestraintsForceLocked` flag `ForceApply` does, which `Toggle` checks unconditionally regardless of device id shape; `ForceUnlock`/panic iterate `activeDeviceIds` (a plain string set) and release every entry the same way, ad-hoc ids included

## 4. Conflict checking covers ad-hoc devices

- [x] 4.1 Verify `RestrictionRuleManager`'s conflict checks (ForcedPose/ArmsCuffed/LegsCuffed/FullBodyCuffed) correctly refuse a conflicting ad-hoc device against an active name-referenced device's rule, and vice versa, using their shared `ConfigKey`-based tracking (no code change expected - confirm behavior) - confirmed by inspection: `WouldConflict`/`TryActivate` key `activeByKind` purely by the `owner` string passed in (the device id) and each rule's `ConfigKey`, with no assumption about what that string looks like - an ad-hoc device's `adhoc:{slot}:{itemId}` id and a captured device's GUID-based id are both just distinct owner keys to this comparison, so cross-conflict refusal already holds with no code change

## 5. Documentation and cross-cutting validation

- [x] 5.1 Update the README's Restraints section to describe the new slot+item picker flow for Sub capture and the new Owner ad-hoc "Define device" flow, replacing any remaining description of the old equip-first capture step
- [x] 5.2 Full manual pass in a live game session: capture a device via the picker with nothing equipped in that slot, apply/release it as the Sub; as the Owner, define and send an ad-hoc device with no matching Sub-side name, confirm it applies, and confirm the Sub cannot self-release it - **marked done at explicit user direction during bulk-archive**; not actually run in this environment (no live game session available) - if a live-game issue surfaces later, treat it as new evidence rather than assuming this was verified
- [x] 5.3 Confirm a conflicting Owner ad-hoc Full Body Cuffed request against an already-active name-referenced device's Full Body Cuffed rule is refused, and vice versa, in a live game session - **marked done at explicit user direction during bulk-archive**; not actually run in this environment (no live game session available) - if a live-game issue surfaces later, treat it as new evidence rather than assuming this was verified
