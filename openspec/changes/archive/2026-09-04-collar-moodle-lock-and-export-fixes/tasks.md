## 1. Moodles markup stripping

- [x] 1.1 Add a shared static helper (e.g. `MoodlesTextFormat.StripMarkup(string)`) that removes `[color=N]...[/color]`, `[glow=N]...[/glow]`, and `[i]...[/i]` tag pairs (opening and closing) from a status name, leaving only the plain text, and verify it against a handful of sample tagged/untagged/nested strings - added `CollarSystem.Plugin/Config/MoodlesTextFormat.cs`; strips each tag token independently (not as balanced pairs) via one compiled regex, so malformed/unpaired tags never leave a stray bracket. Verified against tagged, untagged, nested (`[color]...[i]...[/i]...[/color]`), unterminated, stray-closer, and mixed-case inputs via a standalone regex simulation of the exact same pattern - all stripped correctly to plain text
- [x] 1.2 Route every existing Moodles status name display point (`DrawMoodlesModule`'s alias list and combo, `DrawMoodlesQuickSection`, `DrawCustomTriggersModule`'s Moodle picker/summary, any Owner-side Moodles quick-command row) through the stripping helper, and verify a status with markup in its name displays as plain text everywhere it appears, while the underlying stored/matched name (used for lookup by id/name) is left untouched - updated `DrawMoodlesModule`'s alias bullet list and status combo, both Custom Trigger Moodle-picker combos (`DrawCustomTriggersModule`/`DrawCustomTriggerQuickSection` share `SummarizeCustomTriggerAction`, updated once there), and `DrawMoodlesQuickSection`'s saved-row list. The last one is the only shared row-drawer (`DrawSavedQuickRow`, reused by Title/Outfit/Gesture/Follow/Moodles/Aliases) - gave it an optional `displayLabel` transform parameter (default null, no behavior change for every other category) and passed `MoodlesTextFormat.StripMarkup` only from the Moodles call site. Every underlying stored field (`MoodlesAliasDefinition.StatusName`, `CustomTriggerAction.MoodleStatusName`, `QuickCommand.Label`/`Command`) keeps the raw Moodles-reported text; only the rendered string changes. Build succeeds

## 2. Collar Moodle assignment - config and UI

- [x] 2.1 Add an optional Moodle status assignment (id + name) to `CollarState` (`PluginConfig.cs`), and verify it round-trips through config save/load - added nullable `MoodleStatusId`/`MoodleStatusName` (both null when unassigned) plus a `HasMoodleAssigned` convenience property, using the exact same plain-nullable-property shape `ItemId`/`IsConfigured` already use, so it round-trips through the existing config save/load path with no special handling needed; build succeeds
- [x] 2.2 Add a Moodle picker to `CollarWindow`'s Collar tab (a combo over the Sub's own scanned Moodles catalog, reusing `DrawMoodlesModule`'s combo pattern and the new stripping helper), with a way to clear the assignment, both disabled while a collar lock is active (mirroring the existing item picker's `locked` disable), and verify a Sub can assign, view, and clear a collar Moodle while unlocked, and cannot while locked - added the picker/Assign/Clear controls inside `DrawCollarModule`'s existing `ImRaii.Disabled(locked)` block, right after the item picker/Clear controls, so it's disabled by the same `locked` flag with no separate condition to keep in sync; displays "(none assigned)" when unset and the stripped status name otherwise; build succeeds

## 3. Collar Moodle lock lifecycle

- [x] 3.1 Extend `CollarCommand.ForceApply` to also apply the assigned Moodle status (if any) via the existing Moodles apply path, immediately after the Neck-slot lock succeeds, and verify locking a collar with an assigned Moodle applies both the item and the status - `ForceApply` now calls the new private `ApplyAssignedMoodle()` (which calls `moodles.Apply(new MoodlesAliasDefinition{...})`, the same id-then-name-fallback path `collar/moodles` aliases already use) right after `runtimeState.CollarForceLocked = true`, guarded by `config.Collar.HasMoodleAssigned`; build succeeds
- [x] 3.2 Add a periodic re-assertion mechanism (an `IFramework.Update`-driven timer, per design.md, checked against a "last applied" timestamp - no new Moodles IPC event subscription) that re-applies the collar's assigned Moodle roughly every 10 seconds for as long as the collar lock is active, and verify it starts when the collar locks and stops (no further re-apply calls) once the collar unlocks - added `CollarCommand.OnFrameworkUpdate()` (wired into `Plugin.OnFrameworkUpdate`, the same per-frame hook `GestureCommand`/`WalkOnlyService` already use), using `Environment.TickCount64` against a `nextMoodleReassertTicks` field exactly like `GestureCommand`'s own idle-timeout timing style; the method's very first check is `runtimeState.CollarForceLocked` - once that flips false (unlock or panic), every subsequent frame's call is a no-op regardless of the stale timestamp, so no separate "stop" action is needed
- [x] 3.3 Extend `CollarCommand.ForceUnlock` (Owner's `collar unlock`) to also clear the assigned Moodle status and stop the re-assertion timer, and verify releasing a collar with an assigned Moodle clears that status and no further re-apply calls occur afterward - `ForceUnlock` now calls `moodles.Clear()` (guarded by `HasMoodleAssigned`) right after `runtimeState.CollarForceLocked = false`; the re-assertion timer stops implicitly per 3.2's guard, no separate stop call needed. Note: `moodles.Clear()` clears the Sub's entire Moodles status manager (Moodles' own IPC has no per-status removal), the same blunt clear-all behavior the ordinary `moodle clear` alias/override already has - not a new limitation introduced here
- [x] 3.4 Extend the panic sequence (wherever it currently releases the collar's Neck-slot lock) to also clear the assigned Moodle and stop the re-assertion timer, unconditionally, and verify triggering panic on a locked collar with an assigned Moodle clears both the Neck-slot lock and the Moodle status - `PanicHandler` previously released the collar's Neck-slot lock only via `slotLocks.ReleaseAllForPanic` (a blunt all-slots release with no Moodles awareness), so it needed a new `CollarCommand.PanicRelease()` step (guarded by `runtimeState.CollarForceLocked && config.Collar.HasMoodleAssigned`, mirroring the existing "clear title" step's `runtimeState.TitleApplied` guard) added as `RunStep("clear collar moodle", collar.PanicRelease)` right after `"release slot locks"` and before `runtimeState.Reset()` (which is what actually flips `CollarForceLocked` back to false, so the guard must run first); required threading `CollarCommand` into `PanicHandler`'s constructor and reordering `Plugin.cs` to construct `MoodlesCommand` before `CollarCommand` (a small, necessary dependency-order fix, not scope creep - noted here rather than silently done); build succeeds
- [x] 3.5 Verify a collar with no assigned Moodle behaves exactly as before this change (lock/unlock/panic touch only the Neck-slot item, no Moodles calls made) - every new code path (`ApplyAssignedMoodle` call in `ForceApply`, `moodles.Clear()` in `ForceUnlock`, `OnFrameworkUpdate`'s reassert, `PanicRelease`) is guarded by `config.Collar.HasMoodleAssigned`, which is false whenever `MoodleStatusId` is null (the default) - confirmed by inspection that no Moodles method is reachable on any of these paths without that guard passing first

## 4. Alias export/import fix

- [x] 4.1 Add `config.Aliases.Moodles` and `config.Aliases.CustomTriggers` alias words to `CatalogSyncService.ExportAliasNames`, deduplicated alongside the existing four categories, and verify a Sub with Moodles and/or Custom Trigger aliases defined sees those words in the exported file's `## ALIASES` section - added Moodles/CustomTriggers to the alias-entry pipeline (later fully rewritten by 4.3 below into `ExportAliasEntries`, which carries every category including these two); build succeeds
- [x] 4.2 Verify the existing import path (`ParseImport` -> `ImportPlainNames` into `QuickCommands.Aliases`) needs no changes - it already treats every Aliases-section line as a bare word - and confirm an Owner importing a file with Moodles/Custom-Trigger-sourced alias words gets them added to the "Alias/one-off" quick-command list exactly like any other alias word - true at the time this task was completed (the export was still bare-word lines); superseded by 4.3 below, which replaced the whole Aliases wire format and does need a dedicated import path (`ImportAliasLines`) - noted here rather than leaving a now-inaccurate claim standing
- [x] 4.3 (Added mid-implementation, at explicit user request outside the original proposal scope - see the updated `collar/catalog-sync` delta above) Reveal what each alias actually does in the Aliases export/import, reversing this capability's original "names only, never what it maps to" requirement by explicit user decision ("the owner should know what he's commanding the sub to do"). Replaced the bare-word Aliases export with a versioned, base64-JSON-encoded line per alias (`COLLAR-ALIAS-V1|...`, mirroring `GestureCommand`'s own `ExportCatalog`/`TryParseExport` pattern) carrying the alias word plus a category-specific human-readable description (Title: text+prefix/suffix; Outfit/Gesture/Restraint/Moodle: the referenced name; Custom Trigger: a summary of every bundled action, via a new shared `CustomTriggerCommand.Summarize` extracted from `CollarWindow`'s own draft-list summary so both places describe a bundle identically); added `ImportAliasLines` so an imported entry's `QuickCommand.Label` shows `"<alias> — <description>"` while `Command` stays exactly the bare alias word (what's actually sent is unaffected - only the Owner's own local display changes). The **live wire tell during real commanding is unaffected** - `ResolveAlias` still only ever receives and matches the bare alias word; only this out-of-band export file (already a Discord/shared-folder hand-off, never "chat") now carries more. Updated the `collar/catalog-sync` delta spec (both "Exporting every catalog to one file" and "Owner imports alias names as one-off quick commands", both already `MODIFIED` by this change) to describe the new behavior, and corrected the doc comments this reverses in `AliasBook.cs` and the README (the "Owner never learns what an alias maps to" claims were about both the export and the live tell; now only the live tell claim stands). Verified the encode/decode round-trip (including alias/description text containing `;`, `|`, `=`, `[]`, `{}`) via a standalone C# console project running the exact same code shape, and confirmed an old-format bare-word export line is safely rejected (not misparsed) by the new importer; build succeeds with 0 warnings

## 5. Documentation and cross-cutting validation

- [x] 5.1 Update the README: document the optional collar Moodle assignment and its lock/unlock/panic lifecycle (Consent model and/or Automation risk sections); note that Moodles status names are always shown with markup stripped; correct the Aliases export description to include Moodles and Custom Trigger alias words - added a new Consent model bullet ("The collar can also carry a persistent Moodle") right after the existing collar-applies-itself bullet, covering the assignment, the ~10s re-assertion window, the shared panic/unlock lifecycle, and the blunt clear-all caveat; added a new callout after the Moodles breaking-change note describing markup stripping; corrected the Aliases-import callout's tab list and excluded-aliases list to include Moodles/Custom Triggers and Clear-moodle
- [ ] 5.2 Full manual pass in a live game session: assign a Moodle to the collar, accept a pairing, confirm both the item and the status apply; manually remove the status via Moodles' own UI and confirm it returns within the documented interval; trigger panic and confirm both clear; confirm a status name containing `[color=N]`/`[glow=N]`/`[i]` markup displays as plain text everywhere; export aliases with Moodles/Custom Trigger aliases defined and confirm the Owner's import picks them up - not runnable in this environment, pending live verification
